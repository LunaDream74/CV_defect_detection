# Environments
.venv/
__pycache__/
*.py[cod]

# Data (downloaded, never committed)
data/
MVTecAD/
MVTecAD_lighting/

# Run artefacts.
# Checkpoints stay out: 7.6 MB binaries that `defectloc.train` regenerates.
# Figures and run JSONs stay IN: the README links the figures, and the run JSON
# is the evidence behind every measured row of the results table.
checkpoints/*.pt

# OS / editor
.DS_Store
.ipynb_checkpoints/
