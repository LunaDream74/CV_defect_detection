# Core: everything needed to train, evaluate and reproduce the autoencoder rows
# of the results table. Pinned loosely -- the code is tested against the
# versions in the comments.
torch>=2.0            # tested: 2.10.0+cpu
numpy>=1.24           # tested: 2.4.2
opencv-python-headless>=4.8   # tested: 5.0.0
albumentations>=1.4           # tested: 2.0.8  (v1 and v2 APIs both supported)
pytorch-msssim>=1.0
scikit-learn>=1.3     # tested: 1.9.0
matplotlib>=3.7       # figures only
